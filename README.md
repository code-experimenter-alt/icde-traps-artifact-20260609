# TRAPS ICDE Artifact

This repository contains the submission artifact for:

**TRAPS: One-Sided Learned Admission Indexing for Flood-Resilient Password Verification**

The reviewer-facing quick start is in `ARTIFACT_EVALUATION.md`. The full code package and the minimal Overleaf package are available under `packages/`.

For local verification, run:

```powershell
python experiments/package_submission.py
python experiments/audit_submission_requirements.py
```

